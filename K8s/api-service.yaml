apiVersion: v1
kind: Service
metadata:
  name: school-api-service
  namespace: school
spec:
  selector:
    app: school-api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP